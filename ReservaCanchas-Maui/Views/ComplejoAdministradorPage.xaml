<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ReservaCanchas_Maui.AdminViews.ComplejoAdministradorPage"
             Title="Administrar Complejo">
	<ScrollView>
		<StackLayout Padding="20" Spacing="15">
			<Label Text="Nombre del Complejo" FontAttributes="Bold" />
			<Entry x:Name="NombreComplejoEntry" Placeholder="Introduce el nombre" />

			<Label Text="Imagen del Complejo" FontAttributes="Bold" />
			<Button Text="Seleccionar Imagen" Clicked="OnSeleccionarImagenClicked" />

			<Image x:Name="ImagenComplejoImage" HeightRequest="150" Aspect="AspectFill" IsVisible="False" />

			<Button Text="Guardar Cambios" Clicked="OnGuardarCambiosClicked" />

			<Label Text="Canchas" FontAttributes="Bold" />
			<Button Text="Añadir Cancha" Clicked="OnAniadirCanchaClicked" />

			<CollectionView x:Name="CanchasCollection" SelectionMode="Single" SelectionChanged="OnCanchaSelected">
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<StackLayout Orientation="Horizontal" Padding="10">
							<Image Source="{Binding ImagenCancha}" WidthRequest="50" HeightRequest="50" />
							<Label Text="{Binding NombreCancha}" VerticalOptions="Center" />
						</StackLayout>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</StackLayout>
	</ScrollView>
</ContentPage>
